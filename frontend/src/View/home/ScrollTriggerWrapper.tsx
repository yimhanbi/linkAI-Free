import React from "react";

/**
 * Placeholder for scroll-triggered UI (e.g. GSAP/ScrollTrigger).
 * Replace with full implementation from linkAI repo if needed.
 */
export default function ScrollTriggerWrapper(): React.ReactElement {
  return <></>;
}
